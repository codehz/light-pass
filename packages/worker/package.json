{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "migration:generate": "drizzle-kit generate --dialect=sqlite --driver=durable-sqlite --schema=./src/db.ts --out=./src/migrations",
    "typecheck": "bunx tsc --noEmit",
    "test:core": "bun test ./test/join-request-flow.test.ts ./test/verification-coordinator.test.ts ./test/answer-constraints.test.ts"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.9",
    "typescript": "^5.9.3",
    "wrangler": "^4.69.0",
    "@types/bun": "^1.3.9"
  },
  "dependencies": {
    "@telegraf/types": "^9.2.1",
    "arktype": "^2.1.29",
    "drizzle-orm": "^0.45.1",
    "nanoid": "^5.1.6",
    "workers-cache-storage": "0.1.2"
  }
}
