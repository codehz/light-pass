{
  "name": "light-pass",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "(cd packages/frontend && bun run build) && (cd packages/worker && bun run deploy)"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.7.1"
  },
  "resolutions": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
